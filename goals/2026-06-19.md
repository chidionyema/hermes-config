# Goal of the Day — 2026-06-19

**Goal:** Notification-quality audit + fix (A→B→C). Make every cron alert that reaches the user answer: what's wrong, where, what to do. Dedupe, silence when healthy, fix broken templates, and add a missing daily-goal mechanism.

**Success criteria:**
- A. `<n>` placeholders eliminated; dedup gate installed; `otto-dispatch` timeouts fixed via cron-budget pattern
- B. All notifications useful: one line per real issue, silent when healthy, prose-essay LLM behavior killed
- C. Daily-goal cron asks once at start of day, persists, references in evening reflection

**Receipts required:** every fix ships with before/after probe output.
