model:
  default: claude-sonnet-4-20250514
  provider: anthropic
  api_key: ''
  base_url: ''
terminal:
  backend: local
  timeout: 300
memory:
  memory_enabled: true
streaming:
  enabled: true
  transport: edit
  edit_interval: 1.0
human_delay:
  mode: natural
agent:
  max_turns: 60
platform_toolsets:
  cli:
  - hermes-cli
  telegram:
  - hermes-telegram
delegation:
  max_iterations: 50
  subagent_auto_approve: true
display:
  compact: false
  tool_progress: all
onboarding:
  seen:
    busy_input_prompt: true
