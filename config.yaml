model:
  default: MiniMax-M3
  provider: custom
  api_key: ***PURGED-CREDENTIAL***
  base_url: https://api.minimax.io/v1
terminal:
  backend: local
  timeout: 300
memory:
  memory_enabled: true
streaming:
  enabled: true
  transport: edit
  edit_interval: 1.0
human_delay:
  mode: natural
agent:
  max_turns: 60
platform_toolsets:
  cli:
  - hermes-cli
  telegram:
  - hermes-telegram
delegation:
  max_iterations: 50
  subagent_auto_approve: true
display:
  compact: false
  tool_progress: all
onboarding:
  seen:
    busy_input_prompt: true
